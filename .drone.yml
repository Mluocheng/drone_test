kind: pipeline
type: docker
name: default

steps:
- name: build
  image: node:16.13.2
  pull: always
  commands:
    - pwd
    - npm i
    - npm run build
    - ls -la
      
trigger:
  branch:
    include:
      - master
  event:
    include:
      - push
